# Evals data

| Path | In Git | Notes |
|------|--------|--------|
| `Part 1/` | Yes (full JSONL) | LiveCodeBench v5 + AA-LCR predictions & reviews |
| `MMMU/` | Git LFS | Large embedded images; run `git lfs pull` after clone |

If LFS pull fails on this fork, download from the Google Drive link in the challenge email (see [docs/DATA_SETUP.md](../docs/DATA_SETUP.md)).
